# Project Instructions

## Running Tests
Always use `make test` to run tests. This project uses `uv` as the package manager — never install packages directly with `pip` or `pip3`.

## Running the App
Use `make run` or `uv run python main.py`.

## Python
Use `python3` (not `python`) on macOS. But prefer `make` targets or `uv run` over direct invocation.
